<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.liuliu.ordermanagement.mapper.ProductManagementMapper">

    <resultMap id="ProductMap" type="ProductEntity">
        <id property="id" column="id"/>
        <result property="productCategoryId" column="product_category_id"/>
        <result property="productName" column="product_name"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findProductAndCategory" resultMap="ProductMap">
        SELECT p.*, pc.calculation_type, pc.tax_rate
        FROM products p
        JOIN product_categories pc ON p.product_category_id = pc.id
        WHERE p.id = #{id} AND p.is_deleted = false
    </select>

    <select id="findProductAndCategoryForUpdate" resultMap="ProductMap">
        SELECT p.*, pc.calculation_type, pc.tax_rate
        FROM products p
        JOIN product_categories pc ON p.product_category_id = pc.id
        WHERE p.id = #{id} AND p.is_deleted = false
        FOR UPDATE OF p, pc
    </select>

</mapper>
